#include<semih/competitive.h>

int t,a,b;

int main(){
    fast;
    // test();

    cin>>t;
    while (t--){
        cin>>a>>b;
        if(max(a,b)>min(a,b)*2 or (a+b)%3)
            cout<<"NO\n";
        else
            cout<<"YES\n";
    }
        

    return 0;
}